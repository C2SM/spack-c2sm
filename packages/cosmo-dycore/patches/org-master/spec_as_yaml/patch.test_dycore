diff --git a/dycore/test/tools/test_dycore.py b/dycore/test/tools/test_dycore.py
index bcfcd483..229832a3 100755
--- a/dycore/test/tools/test_dycore.py
+++ b/dycore/test/tools/test_dycore.py
@@ -21,7 +21,13 @@ def main():
     parser.add_argument('-b', '--base_directory', type=str, help='Dycore build directory', required=True)
     args=parser.parse_args()
 
-    dycore_spec = spack.cmd.parse_specs(args.spec)[0]
+    try:
+        # assume spec is passed as a yaml-file
+        with open(args.spec,"r") as f:
+          dycore_spec = Spec.from_yaml(f)
+    except:
+        # assume spec is passed as a string
+        dycore_spec = Spec(args.spec)
     dycore_spec.concretize()
 
     test_dycore(dycore_spec, args.base_directory, args.data_path)
