pipeline {

    agent {
        node {
            label 'daint'
        }
    }
    stages {
            stage('Install Upstream Base') {
                steps {
                    sh"""
                    . ./setup-env.sh
                    spack env activate upstreams/daint/base
                    spack install -v
                    """
                }
            }
            stage('Install Upstream Exclaim') {
                steps {
                    sh"""
                    . ./setup-env.sh
                    spack env activate upstreams/daint/exclaim
                    spack install -v
                    """
                }
            }
    }
    post {
        always {
            echo 'Cleaning up workspace'
            deleteDir()
        }
    }

}

