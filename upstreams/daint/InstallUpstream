pipeline {

    agent {
        node {
            label 'daint'
        }
    }
    stages {
            stage('Install Upstream Base') {
                steps {
                    sh"""
                    . ./setup-env.sh
                    spack env activate upstreams/daint
                    spack install -v
                    """
                }
            }
            stage('Install Upstream Rttov') {
                steps {
                    sh"""
                    . ./setup-env.sh
                    spack env activate upstreams/daint/rttov
                    spack install -v
                    """
                }
            }
    }
    post {
        always {
            echo 'Cleaning up workspace'
            deleteDir()
        }
    }

}

