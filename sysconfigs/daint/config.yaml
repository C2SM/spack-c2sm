config:
  build_jobs: 4
  build_stage:
  db_lock_timeout: 10
  extensions:
  install_tree:
    projections:
      all: '{name}/{version}/{compiler.name}/{hash}'
    root: 
  module_roots:
    tcl: 
  source_cache: ~/.spack/source_cache    
  template_dirs:
  - $spack/templates
