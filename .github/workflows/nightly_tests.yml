name: Nightly tests
on:
  schedule:
    - cron: '12 11 * * *'  # Run this workflow every day at midnight
  workflow_dispatch:

jobs:
  spec-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Test echo
        run: echo "Hello"